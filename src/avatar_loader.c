/*retorna null por padrão*/
#include <stdio.h>
#include <stdlib.h>

#include "avatar_loader.h"

char** load_avatar_from_file(const char* path, int* lines)
{
    (void)path; (void)lines;
    return NULL;
}
